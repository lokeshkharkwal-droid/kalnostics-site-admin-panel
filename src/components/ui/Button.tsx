/**
 * Compatibility re-export for the ported Advance PDF module. The repo's real
 * Button (`@/shared/ui`) already accepts the `variant` (`secondary` etc.),
 * `onClick`, and `disabled` props the ported components use, so it is a
 * drop-in replacement.
 */
export { Button } from '@/shared/ui'
